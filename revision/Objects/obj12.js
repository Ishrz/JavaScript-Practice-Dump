const locations={
    city:"pune",
    coordinate:{
        lat:23.2,
        lng:77.4,
    },
};

let {city}= locations;
let {lat}= locations.coordinate;
console.log(`The city name is = ${city} and the lattitude is = ${lat}`);